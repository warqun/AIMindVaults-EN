---
type: version-log
tags:
  - AIMindVault
  - root
updated: 2026-04-29
---

# Root Version Log

History of root-level changes in the multi-vault. Tracks changes under `.claude/`, `.antigravity/`, root configuration files, etc.

## Deployment Status Columns (introduced 2026-04-15)

- **Korean**: Reflected in `C:\SellingVault\Korean\AIMindVaults`
- **English**: Reflected in `C:\SellingVault\English\AIMindVaults`
- Values: `✅` reflected / `🕓` pending / `–` not applicable to that language deployment (creator-only or custom rule)
- Entries before 2026-04-15 predate this schema and are left blank.

## New Schema (after 2026-04-15)

| Version | Date | Korean | English | Change |
|---------|------|--------|---------|--------|
| R092 | 2026-04-29 | 🕓 | 🕓 | **New `/delegate-task` skill — agent-delegation workflow SOP.** Built on `/spawn-claude` (CLI spawn mechanism), bundles the full delegation workflow (queue file → spawn → trigger → tracking → completion handling) into a standard SOP. Pattern validated through the 4-29 English-deployment review delegation case; core to the user's "agent collaboration infrastructure" vision. (1) New `.claude/commands/custom/delegate-task.md` — trigger keywords (delegate / hand off / background / worker / parallel / concurrent), delegate-suitable vs unsuitable work types, 5-step workflow (queue frontmatter + cold-start self-contained body → `/spawn-claude` invocation modes 1·2·3 + parallel multi-spawn → trigger-message pattern → tracking → completion-trigger file), reminder of concurrent-edit zones (agent-ownership.md), cold-start self-containedness guide, multi-session handoff via self-memo, rule integration (distribution-sync · script-creation-approval · agent-ownership), 3 usage examples (English-deploy review on 4-29 / leftover cleanup / parallel domain-vault index rebuild), direct-work vs delegate decision table. (2) `.claude/rules/core/_skill-router.md` adds the "Agent task delegation" trigger row. The custom skill itself is irrelevant to deployment, but `_skill-router.md` is core, so Korean/English deployment is pending. Worked by: Claude |
| R091 | 2026-04-29 | ✅ | 🕓 | **New Stop hook — `check_status_on_stop.py` (advisory; reminds about `_STATUS.md` / `_SESSION_HANDOFF` updates).** Implements recommendation #5 (verify `_STATUS.md` at session end) from the self-memo `_AGENT_COMMS/to_claude/20260424_Claude_Claude_post_bulk_registration_hook_implementation_proposal.md`. (1) New `.claude/hooks/check_status_on_stop.py` — on Stop event, runs `git diff --name-only HEAD`. If files changed but `_STATUS.md` or `_SESSION_HANDOFF_*.md` is unmodified, prints an advisory to stderr (exit 0, no blocking). Uses `stop_hook_active` to prevent infinite loops. (2) `.claude/settings.json` registers a new `hooks.Stop` entry with no matcher, applied to all Stop events. Coexists with existing `hooks.PreToolUse.Bash` (block_nul_redirection.py). **First introduced as advisory only** (no deterministic blocking) — can later be hardened to `decision: block` after infinite-loop risks are verified. Worked by: Claude. Deployment pending — the hook file is itself a `.claude/hooks/` deployment target (per the R073 pattern). |
| R090 | 2026-04-29 | – | – | **New `Setup New Device.{bat,command,sh}` — combined per-device bootstrap wrapper (not deployed).** A single launcher to bootstrap a new device right after `git clone`. Flow: verify Node.js → check CoreHub CLI → ensure `node_modules` + `npm install` → `/register-vaults` dry-run + user confirmation + `--apply` → `sync-all`. Device-agnostic (uses `%~dp0` on Windows / `$(dirname "$0")` on Unix to locate itself). Three platform variants (`.bat` Windows · `.command` macOS double-click · `.sh` Linux/Unix). **Tracked in personal git** (notebook ↔ desktop device sync) + **automatically excluded from SellingVault** (via `deploy-dist.js` whitelist mechanism — anything not in `DEPLOY_TARGETS` is dropped). The distributed bundle keeps the components only (`/register-vaults` skill + `sync-all` CLI + `Sync All Vaults.{bat,command,sh}`); the combined wrapper is left to user discretion (Unix philosophy). Briefly added to `.gitignore` mid-task and removed immediately after the user pointed out the device-sync risk. Side task: deletion of remote worktree branch `claude/hardcore-lichterman-d15740` (with explicit user approval, after fully-merged verification, via `git push origin --delete`). |
| R089 | 2026-04-29 | – | – | **Added Codex `_AGENT_COMMS` reference shim** — new `.codex/rules/agent-comms.md`. The shared protocol canon stays in `_AGENT_COMMS/README.md` and `.claude/rules/custom/agent-ownership.md § Inter-agent communication`; the Codex side gets a thin application rule rather than a duplicate. Specifies that Codex's inbox is `to_codex/`, outbox is `to_claude/`, and `to_claude/` is read only when verifying responses to Codex-originated requests / archive checks, or on explicit user instruction. Pins to Codex-only rules: bulk frontmatter check at session start, "report `open`/`in-progress` then wait for user instruction" flow, and the rule that `resolved`/`archived` items must not be misclassified as new queue items. `.codex/rules/agent-rules.md` adds `agent-comms.md` to its Codex-specific rule list. `_AGENT_COMMS` is a creator-only communication space, so it is irrelevant to Korean/English deployment. |
| R088 | 2026-04-27 | ✅ | 🕓 | **PowerShell permission cleanup + new Obsidian instance-control rule/skill (B-3) — worktree merge (originally R080 → renumbered R088).** Merged commit `3538a0d6` from notebook worktree branch `claude/hardcore-lichterman-d15740`. The original worktree recorded this as R080, which collided with the main branch's R080 (Sync This Vault `.sync/` move) and R087 was already taken, so it was renumbered to R088. Contents: (1) **Added PowerShell allow** — registered `"PowerShell"` + `"PowerShell(*)"` under `permissions.allow` in `.claude/settings.json`. (2) **Re-defined the B-3 direction** — abandoned the original "OS-process-level auto-block / auto-close" approach and switched to "AI opens/closes per the user's explicit N (user-feel based)". (3) **New `.claude/rules/custom/obsidian-instance-control.md`** — trigger keywords, detection method (`Get-Process Obsidian` + non-empty `MainWindowTitle`), close flow (graceful close → `CloseMainWindow` API → 5-second timeout), open flow (URI vs direct-launch branching), `obsidian.json` `open` flag sync. (4) **New `.claude/commands/custom/obsidian-windows.md`** — `/obsidian-windows` skill (`N` · `close <vault>` · `close all` · `open <vault>` · `status` arguments). (5) **`.claude/rules/core/_skill-router.md` adds a new trigger row** — "Obsidian instance control" → `/obsidian-windows` + `obsidian-instance-control.md`. settings.json and the custom rule/skill are not deployment targets; only `_skill-router.md` is core, so Korean/English deployment is pending. |
| R087 | 2026-04-27 | – | – | **Strengthened `agent-ownership.md § _AGENT_COMMS` — added bulk-frontmatter check + queue-trigger classification SOP.** On 2026-04-27, after a notebook → desktop pull, `_AGENT_COMMS/to_claude/` accumulated 11 items (8 resolved + 3 open). The AI reported "all 8 are queued tasks" without checking frontmatter. Root cause: the rule lacked a "trigger ≠ pending task" classification SOP. Mitigation: (1) the `Session-entry scan` section now mandates "if there are many items (3+) or new items appear right after a pull, do a bulk frontmatter check before reporting." (2) New `Bulk frontmatter check (mandatory)` subsection — Grep/bash commands + report-format template (split open / in-progress / resolved + classify self-sent / counterpart-sent / completion-trigger). (3) New `Queue trigger classification (mandatory)` subsection — 4-type table (new task queue / self-sent self-memo / completion trigger / user message) + processing direction + explicit ban on guess-based "queued" reports. Plan note: under the creator's project notes (Phase 1 immediate, Phases 2-3 after cases accumulate). Personal operations rule — irrelevant to Korean/English deployment (custom/). |
| R086 | 2026-04-25 | ✅ | 🕓 | **Domain Preset (ZK) tag-rule clarification — added AI-Draft stage + reinforced root-discovery path.** Codex-queue response. Cause: Codex created new domain notes with only `tags: [zk]`; the user requested a check on whether this conformed to the canon, what rule conflicts existed, and whether onboarding harness had a flaw. Analysis: the Domain Preset canon (`BasicDomainVault/CLAUDE.md`) frontmatter standard covered only the 5 formal ZK types (zk · literature · synthesis · moc · pattern) and **omitted the AI-Draft stage (`Contents/_AI_Drafts/`) temp-note marker**, plus the tension between the root general `note-writing` and the Domain Preset's ZK principles (which canon to follow lacked a clear discovery path). Mitigation: (1) `BasicDomainVault/CLAUDE.md § Tag rule · Frontmatter standard` extended — explicit `ai-draft` / `temp` operational-stage tags, allowed parallel use with formal types, recommend `tags: [ai-draft]` or `[zk, ai-draft]` for AI-Draft folder notes, remove `ai-draft` when promoted to a formal type. (2) Root `_essentials.md § 6` adds a new "ZK system (Domain Preset family) tag rule" section — uses ZK operational/state tags only instead of the general tag rule, expresses concepts via wikilinks + MOC, with the canon explicitly in the vault's CLAUDE.md. **Automated validation (post-edit-review whitelist) was not adopted** (semantic complexity vs ROI; Codex agreed). Tag fixes for the 4 AI-Draft notes are within Codex's domain (single-vault note edits per the agent-ownership rule) — Codex self-corrects after reading R086. |
| R085 | 2026-04-25 | ✅ | 🕓 | **Simplified `AGENT_ONBOARDING.md § 2 Vault structure` to the Multi-Hub 3-tier layout.** R084's first-pass generalization (category-pattern table) was still leaking user-specific conventions (`Domains_*`, `Lab_*`, `Projects_*` etc.) per user feedback. The actual product structure is the 3-tier Core Hub / Preset Hub / satellite vault, which is more natural to state explicitly and more intuitive for new users. Category naming (`Domains_Game`, `Lab_Infra`, etc.) is an organizational pattern users decide for themselves, not a product-mandated structure. R085 rewrites § 2 as a 3-tier table + behavior flow (Core → Preset → satellite sync direction) + how to add a vault (skill invocation). Auto-applied to the SellingVault deployment on the next `/distribute`. |
| R084 | 2026-04-25 | ✅ | 🕓 | **Stopped exposing personal assets in `AGENT_ONBOARDING.md` + new distribution content-safety rule.** User-discovered: the `AGENT_ONBOARDING.md § 2 Vault structure` table cited personal vault names as examples, leaking the author's personal context to new SellingVault users. Mitigation: (1) Generalized `AGENT_ONBOARDING.md § 2` — replaced the category table with a pattern + use-case description, and pointed users at the `_STATUS.md` vault registry as the source of truth, with an explicit "no citation of personal assets" warning. (2) New `.claude/rules/core/distribution-content-safety.md` — classifies deployment-target vs non-target documents, conventions for category examples / keyword mappings / changelog entries, an Incident Rule for pre-checking new or modified deployment-target documents, an automated-verification PowerShell snippet, and a separate-task note for cleaning up the SellingVault distribution. (3) `.claude/rules/MANIFEST.md` registers the new file in the core/ table. **Follow-up SellingVault cleanup**: on the next `/distribute`, bulk-check and generalize `AGENT_ONBOARDING.md` / `CLAUDE.md` / `AGENTS.md` / `_STATUS.md` in both Korean and English. Always-injected on every agent session going forward to prevent recurrence. |
| R083 | 2026-04-25 | ✅ | 🕓 | **Reflected the R081 "canon-first" rule into Claude territory (Codex → Claude queue response).** After Codex hardened `_essentials.md § 4` and `rules-archive/edit-mode-separation.md` with the canon / propagated-copy / generated-artifact split in R081, the Claude side took the queue. Result: (1) `_skill-router.md` needs no change (it already maps to `_essentials.md`, which now includes R081). (2) Added a one-line reminder to `CLAUDE.md § Mandatory agent compliance` — "When editing the workspace (R081): classify as canon / propagated copy / generated artifact, modify the canon and the installer/sync logic first, then verify version signals + idempotency." (3) Added a "Canon / Propagated copy / Generated artifact split" section to `.claude/commands/core/hub-broadcast.md` — reminds the agent to confirm the target file is the canon before invoking broadcast (prevents stale copies on satellites when broadcasting a generated artifact). archive needed no change since Codex had already hardened it. Result: the R081 principle is now auto-reminded across all Claude sessions, with an additional reminder right before sync-tool invocation. |
| R082 | 2026-04-25 | – | – | **New manufacturing / machine-assembly domain vault.** A new domain vault for the user's upcoming three-month workshop / industrial machine-assembly engagement. Cloned the `BasicDomainVault` template as an `AIHubVault_Domain` satellite under `Vaults/Domains_Manufacturing/`, and registered the new category. Vault scope: fasteners (wrench bolts, etc.), basic tools, drawings & tolerances, mechanical elements of automation equipment, on-site assembly procedures, and inspection criteria. Following the AI-only-draft principle, the initial study guide and the Naver-blog / PDF reference queue the user supplied are isolated under `Contents/_AI_Drafts/`. Updated routing keywords in root `CLAUDE.md` / `AGENTS.md` and the `_STATUS.md` registry. |
| R081 | 2026-04-25 | ✅ | 🕓 | **Strengthened the workspace-mode "canon / propagated / generated" split.** In a Multi-Hub sync setting, before any workspace edit, files must be classified as canon (source of truth) / propagated copy (synced copy) / generated artifact (installer/sync output). The shared and Codex rules were updated accordingly: instead of editing the propagated copy / generated artifact directly, modify the canon, template, or installer/sync logic first, then verify the version signals (`bump-version --broadcast`, Preset `_WORKSPACE_VERSION.md`, root `_ROOT_VERSION.md`) and the final installation location / idempotency. Exceptions where breaking the rule is more profitable must be reported up-front (reason · scope · alternative) and confirmed by the user. |
| R080 | 2026-04-25 | ✅ | 🕓 | **Moved the "Sync This Vault" launcher into `.sync/`.** To prevent Obsidian sidebar pollution, removed `Sync This Vault.bat/.command/.sh` (3 forms) from every vault root and reinstalled them under `<vault>/.sync/Sync This Vault.*`. Hardened the CoreHub CLI's `install-launchers` / `sync-all` / `sync-workspace` commands to install from the Hub template, preventing reuse of stale local templates. Sync log location is `<vault>/.sync/sync.log`. CoreHub `_WORKSPACE_VERSION.md` recorded 202604240003~0005 + completed `core-sync-all --broadcast`; the 6 Preset Hubs manually recorded `_WORKSPACE_VERSION.md` 202604240003 followed by a successful re-validation across all 40 vaults. SellingVault deployment is on a separate `/distribute` task. |
| R079 | 2026-04-24 | ✅ | 🕓 | **Introduced manual sync launchers.** So that non-developer users can run a workspace sync by double-click, added template `Sync All Vaults.bat/.command/.sh` at the root and `Sync This Vault.bat/.command/.sh` per vault. Added `sync-all` (iterate all vaults, install missing `node_modules`, run `pre-sync`, append to `sync.log`, summarize success/failure) and `install-launchers` (install root + per-vault launchers) commands to the CoreHub CLI. Wired `sync-workspace` to install per-vault launchers automatically after PULL/VERIFY/PLUGIN_ONLY. Root `README.md` documents the manual-sync flow. SellingVault targets: root `Sync All Vaults.*` and the CoreHub template / CLI changes inside BasicVaults — actual deployment sync is performed in a separate `/distribute` task. |
| R078 | 2026-04-22 | – | – | **New personal-operations vault (Projects_Personal category).** Reflects the user's directive that AIMindVaults product development and personal canonical-vault management must be separated **at the vault level**. Created via `/clone --hub AIHubVault_Project` — first real-world test of the R076 `defaultTemplate` mechanism (auto-resolves to `BasicProjectVault`, mirror-copies 345 files, writes make-md systemName, auto-creates `.sync/hub-source.json`). Registered the new category and updated the root `_STATUS.md`. Memory feedback rule added so that future agents always distinguish AIMindVaults-product context from personal-operations context. Use cases: review / Preset-conversion decisions for satellites, personal rebase plan, content migration. **Not a Korean/English deployment target** (personal-operations content; expected to be excluded via `.gitignore`, preventing SellingVault contamination). |
| R077 | 2026-04-22 | ✅ | ✅ | **Step 3 complete — created the four BasicXxxVault clone templates (one per Preset).** Realized the substance of the R076 `defaultTemplate` mechanism. Generated the per-Preset clone templates via 4 parallel-dispatched agents (BasicDiaryVault · BasicDomainVault · BasicLabVault · BasicProjectVault). (1) **BasicDiaryVault** — Daily/Weekly/Monthly/Yearly flat structure under Contents/ · 5 Templater (Morning/Evening/Weekly/Monthly/Yearly) · Dashboard.md with 6 queries. (2) **BasicDomainVault** — Zettelkasten 8-folder layout (00_Fleeting~05_Pattern + _AI_Drafts + _archive) · 5 Templater (fleeting/literature/permanent/moc/synthesis) · Dashboard.md 6 queries · `Contents/_log.md` + `_index.md` skeleton. (3) **BasicLabVault** — R&D 9 types (idea/literature/permanent/moc/experiment/failure-log/plan/report/decision) · 3-axis folders · 9 Templater · Dashboard.md 9 queries. (4) **BasicProjectVault** — PARA 4 (Projects/Areas/Resources/Archive) coexisting with 9-type folders (00_Idea~08_Retro) · 9 Templater · Dashboard.md 8 queries. All four were mirror-cloned from `BasicContentsVault` and customized to the Preset spec. Common: hub-marker.json / hub-source.json not generated (Template principle preserved); `.obsidian/plugins/` inherits from BasicContentsVault (Hub sync expected to prune); `_Templates/` placed at root; Templater folder bindings / QuickAdd are per-device (set up manually). **Verification**: all 6 Preset Hubs × `defaultTemplate` resolve correctly (default/minimal → BasicContentsVault · diary/domain/lab/project → their dedicated Basic templates, promoted from FALLBACK to the formal resolution). First session under the new operating mode (2026-04-22): "for safe autonomous tasks with a written plan, run parallel dispatch immediately; for risky/judgment items, batch into a deferred list and report at the end". **Combined deferred items** (awaiting user judgment): (A) Custom plug-ins inherited from BasicContentsVault — whether to prune the mismatch with each Preset's spec (auto-cleanup expected during `AIHubVault_*` rebase). (B) Diary flat layout vs `Contents/Diary/Daily/` wrapper — final decision. (C) Verify estimated body of the Diary Monthly/Yearly templates. (D) Diary `aimv sync` exclusion flag (spec § 10 recommendation). (E) Project `_log.md` / `_index.md` inclusion (currently excluded). (F) Project `Tags/TAGS.md` flat-rule rewrite. (G) Per-vault `_Standards/` slimming for Preset-only content (vs BasicContentsVault inheritance). (H) Pre-injected Templater folder bindings / QuickAdd (currently per-device). Korean/English deployment pending — the four BasicXxxVaults are inside BasicVaults and are auto-included by deploy. |
| R076 | 2026-04-22 | ✅ | ✅ | **Relocated the BasicVaults launcher + introduced the `defaultTemplate` mechanism (Multi-Hub improvement).** Relocated the launcher (`MakeCloneVault.bat/.sh`) from inside each Hub (7 × 2 = 14 copies) and 24 satellite stragglers → to a **single copy** at `Vaults/BasicVaults/` root. Plan note: `Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/plan/architecture/20260422_BasicVaults_launcher_relocation_plan.md`. (1) `clone-vault.js` — added `MakeCloneVault.bat/.sh` to `CLONE_EXCLUDE_FILES` (prevents future satellite contamination); introduced source-resolution precedence: `--source-path` > Hub's sibling `defaultTemplate` > `BasicContentsVault` fallback > CLI auto-detect. New helpers `resolveHubDefaultTemplate()` · `resolveBasicContentsFallback()`, plus `[1/4]~[4/4]` step-number consistency and source-origin logs. (2) `hub-marker.schema.json` — new `defaultTemplate` field (string; supports name / relative path / absolute path; documented inline). (3) `config.js` — removed `MakeCloneVault.{bat,sh}` from `CORE_PATHS` (a single copy no longer needs Hub-to-Hub propagation; deploys via the `Vaults/BasicVaults` dir-mirror in DEPLOY_TARGETS). (4) New `Vaults/BasicVaults/MakeCloneVault.bat/.sh` — auto-scan sibling Hubs (only folders with `.sync/hub-marker.json`) + numbered menu + Hub/Parent/Name prompts → invoke `node clone --hub` (CLI auto-resolves defaultTemplate). The CoreHub CLI is the single canon. (5) Added `defaultTemplate` to the 6 Preset Hubs' `hub-marker.json`: default/minimal = BasicContentsVault · diary = BasicDiaryVault · domain = BasicDomainVault · lab = BasicLabVault · project = BasicProjectVault (the latter four are created in Step 3; BasicContentsVault fallback applies until then). (6) Cleanup: removed `.sync/MakeCloneVault.*` from CoreHub + 6 Presets + BasicContentsVault (15 files) + every stray `.sync/MakeCloneVault.bat` from the 24 satellites (27 files; legacy contamination from BasicContentsVault clones, all broken so functionally irrelevant) → final `find` shows only the two files at `BasicVaults/MakeCloneVault.{bat,sh}`. (7) `bump-version --broadcast` from CoreHub → 18 items propagated to the 6 Preset Hubs (clone-vault.js + config.js + hub-marker.schema.json). (8) Updated the `/create-preset-hub` skill: `defaultTemplate` field spec + resolution-rule table. **Step 3 follow-up**: create BasicDiaryVault · BasicDomainVault · BasicLabVault · BasicProjectVault (each per its Preset spec for folder structure, Templater, QuickAdd, Dataview). Korean/English deployment pending. |
| R075 | 2026-04-22 | ✅ | ✅ | **New `/create-preset-hub` skill + clarified `create-hub.js` comments.** Documented the Preset Hub creation workflow as a skill. (1) New `.claude/commands/core/create-preset-hub.md` — explains where target-path goes, the `--from` auto-detect formula, the hubId required-rule, the 5 automatic steps in `create-hub` CLI (mirror copy · per-device cleanup · `hub-marker.json` · entry-file rewrite · make-md systemName), the manual follow-ups (`coreHubVersion` entry · Custom plug-in install · `community-plugins.json` · templates/QuickAdd/Dataview · root `_STATUS.md` / `_ROOT_VERSION.md` registration), the 3-tier sync flow (Core push · Custom internal · satellite binding), the 3 satellite-binding methods (`clone --hub` · `rebase` · manual `hub-source.json`), the 4-step verification, the 5 prohibitions, and cross-links. (2) Hardened comments in `Vaults/BasicVaults/CoreHub/.sync/_tools/cli-node/src/commands/create-hub.js` — added 4 top-of-file blocks ("Path resolution" · "5-step workflow" · "Manual follow-ups" · "Sync semantics after creation"), the `__dirname`-relative 5-step ascent formula (src → cli-node → _tools → .sync → HubRoot), per-constant rationale (per-device / CLONE_EXCLUDE) comments, unified [1/5]~[5/5] step labels (the previous mix of [1/4] · [2/4] · [3/5] is fixed), and per-step block descriptions (why · auto/manual · scope). (3) `.claude/rules/core/_skill-router.md` — added the "New Preset Hub" row (triggers: Preset Hub creation, create-preset-hub, create-hub, new Hub, AIHubVault_ creation, Hub derivation), removed `create-hub` / `Preset Hub` duplicates from the existing Multi-Hub row, and clarified the "New vault" row as "(satellite)". (4) `.claude/commands/MANIFEST.md` — added create-preset-hub.md to the core table; clarified create-vault.md's purpose as "(satellite)". Korean/English deployment pending — both the skill and `create-hub.js` comments deploy automatically since CoreHub is inside BasicVaults. |
| R074 | 2026-04-21 | ✅ | ✅ | **Fixed the `block_nul_redirection.py` hook output format — operation verified.** Discovered that the Claude Code `PreToolUse` hook from R073 was not actually blocking. Cause: the script returned the legacy flat JSON `{"decision": "deny", "reason": "..."}` with exit 0, which Claude Code did not recognize and let through. Fix: (1) switched to the current nested schema `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "..."}}`; (2) also writes the same reason to stderr; (3) exits 2 to make the blocking error explicit (nested JSON + exit 2 + stderr — three confirmation paths). Live test passes: `echo x 2>nul` · `echo x >nul` · `echo x 2>NUL` are all blocked + no `nul` file created + `> /dev/null` (safe) goes through. Reference: docs.claude.com/en/docs/claude-code/hooks. Korean/English deployment pending — the hook file is a deployment target, so both sides need updating. |
| R073 | 2026-04-21 | ✅ | ✅ | **Multi-layer defense hooks/templates deployed + major reinforcement of `shell-redirect-safety.md`.** Reflects the 2026-04-21 deep-research findings. (1) **Claude Code `PreToolUse` hook**: new `.claude/hooks/block_nul_redirection.py` — detects `2>nul` / `>nul` / `&>nul` patterns right before Bash tool invocation and returns `decision: deny`. Registered under `hooks.PreToolUse.Bash` matcher in `.claude/settings.json`. Deterministic blocking from session restart onwards. (2) **5 CI templates** (`.claude/templates/ci/`): `check-reserved-names.sh` (shared scanner; reused by Git hook + CI), `pre-commit-reserved-names.sh` (Git pre-commit wrapper), `deny-reserved-names.yml` (GitHub Actions workflow), `bashrc-nul-trap.sh` (Bash DEBUG trap), `vault-watcher.js` (Node `fs.watch` real-time watcher). Installed manually or via the distribute skill into the deployment repos. (3) **Major reinforcement of `.claude/rules/core/shell-redirect-safety.md`**: explanation of the root cause (intentional POSIX-compatibility design of Cygwin/MSYS2), reinterpretation of the PowerShell FileSystemProvider early-termination (it is FileInfo materialization failure obscuring subtrees, not phantom matching), 5-layer defense deployment (agent / shell / repo / CI / filesystem) with locations, full Windows reserved-name table (NUL · CON · PRN · AUX · COM1-9 · LPT1-9), Obsidian Shell Commands audit Incident Rule, deletion-throughput ranking (native Rust > PS `[System.IO.File]::Delete('\\?\...')` > cmd del), and deep-research cross-link. Expanded from ~10 lines to 164. Korean/English deployment pending — hooks/templates/rules are all general-purpose, so both targets apply. |
| R072 | 2026-04-21 | ✅ | ✅ | **Added the PowerShell `Get-ChildItem -Filter nul` ghost-match Incident Rule to `shell-redirect-safety.md`.** Reflects the 2026-04-21 investigation: PowerShell `-Filter nul` returns fake `FileInfo` objects from every directory due to how the Windows `FindFirstFile` API handles reserved device names (41,440 ghosts vs zero real files). Provides the 4 correct scan methods (cmd `dir /A /B /S` 1st, Python `os.walk` 2nd, Rust `walkdir` 3rd, PowerShell without Filter 4th), the verification method (`[System.IO.File]::Exists('\\?\...')` + Delete-exception-message discrimination), and the 2026-04-21 Incident link. Korean/English deployment pending — shell-safety rule, applies to both targets. |
| R071 | 2026-04-21 | ✅ | ✅ | **Plugin-seed bootstrap + Hub `systemName` consistency repair.** Cause: confirmed that Preset Hub creation violated the "make-md `systemName` must match the vault name" rule (`rules-archive/vault-individualization.md`). When CoreHub was forked from AIHubVault, `data.json` was copied but the `systemName` rewrite was missed, leaving "AIHubVault". The 5 Preset Hubs (Minimal · Diary · Domain · Lab · Project) were forked via `create-hub` during the Core 6 era (before make-md was included), so they lack `data.json` entirely. Root cause: `create-hub` Step 1 (`mirrorDirectory`) does not create files missing from source, and `core-sync-all` does not propagate `data.json` outside of the `CORE_FORCE_DATA_JSON` whitelist (by design, to protect personal settings). Mitigation: (1) New `src/lib/plugin-seed.js` — `seedPluginDataIfMissing()` + `seedPluginDataBatch()` + `normalizePluginData()` (includes make-md-specific `systemName` rewrite). Policy: seed only when `data.json` is **absent** from the target; never overwrite an existing file (conservative). (2) `core-sync-all.js` adds a "Plugin Data Seed" step iterating the entire `CORE_PLUGINS` array. New Core plug-ins added later are automatically bootstrapped on existing Presets. (3) Manually fixed CoreHub `data.json` `systemName` ("AIHubVault" → "CoreHub"). (4) `bump-version --broadcast` propagated to 6 Presets → make-md/`data.json` is generated on the 5 Presets and `systemName` is auto-rewritten to the vault name (e.g., "AIHubVault_Diary", "AIHubVault_Minimal"). Result: all 7 Hubs have `systemName` = vault name. Future Core plug-in additions auto-seed onto existing Presets. After R070 included make-md in the Core Hub, `create-hub` works normally via Step 1 (`data.json` copy) + Step 5 (`systemName` rewrite). Korean/English deployment pending. |
| R070 | 2026-04-21 | ✅ | ✅ | **Core 7 lineup + auto-merge logic for `core-sync-all`.** (1) Added `make-md` to the CoreHub `CORE_PLUGINS` array (Core 6 → Core 7). Rationale: in Preset Hub environments, make-md visibility (sidebar navigation, folder tree, tag spaces) is core UX, so all Presets need it forced. Installed make-md (5.7 MB / 4 files) under CoreHub `.obsidian/plugins/` + activated in `community-plugins.json` + updated `CLAUDE.md` "Core 7" reference. (2) New `mergeCommunityPlugins()` in `src/commands/core-sync-all.js` — reads each Preset's `community-plugins.json` and merges as `[...CORE_PLUGINS (config order), ...existing Custom (preserve order)]`. Previously, Core plug-in files were copied but the active list still had to be edited manually. (3) `bump-version --broadcast` propagated make-md files + `community-plugins.json` merge to all 6 Preset Hubs (Default · Minimal · Diary · Domain · Lab · Project), syncing 38 items. Future Core plug-in additions auto-activate. Korean/English deployment pending — CoreHub itself is included in BasicVaults and ships with deploy. |
| R069 | 2026-04-21 | ✅ | ✅ | **New shell-redirect safety rule.** New `.claude/rules/core/shell-redirect-safety.md`. Driven by the 2026-04-21 incident (CMD-syntax `2>nul` mixed into Git Bash created 69 empty `nul` files across AIMindVaults). Provides per-shell NUL-redirect syntax tables (Bash `2>/dev/null` · PowerShell `2>$null` · CMD `2>nul`), shell-identification criteria (Bash tool → Git Bash / `powershell` · `pwsh` → PowerShell / `cmd /c` → CMD), a Windows Incident Rule (no `2>nul` in Bash; delete `nul` files using `\\?\` UNC prefix + `[System.IO.File]::Delete()`), and the cross-platform principle (Bash syntax by default in deployable scripts). `.claude/rules/MANIFEST.md`'s core table adds the new row in alphabetical order between `script-management.md` and `temp-file-management.md`. Always-injected from the next session. Korean/English deployment pending — general safety rule, applies to both targets. |
| R068 | 2026-04-20 | ✅ | ✅ | **Added an "Intent-based entry guide" section to the root CLAUDE.md.** Adopted the navigation pattern from the Claude.md of the obsidian-pllc Gumroad template (a hypothetical-law-firm example vault). Maps 11 intent categories along the time / object / action axes (state check · session context · new note · search · domain reference · project · Multi-Hub change · deployment · rule update · personal record · agent collaboration) onto specific entry files / commands. The existing keyword mapping (vault-ID level) is kept as a secondary layer used when intent is unclear. Related research notes: Zettelkasten / Diary Hub deep-research prompts and the Diary Hub spec. |
| R067 | 2026-04-20 | ✅ | ✅ | **Multi-Hub Phase 1 final completion — review passed + session end.** (1) Updated `.claude/commands/core/create-vault.md` — Hub-selection UX + `--hub` option examples (CoreHub · AIHubVault). (2) Updated the root `_STATUS.md` — registered CoreHub (Core Hub, hubId=core) · AIHubVault_Minimal (Preset, hubId=minimal) in the BasicVaults registry; changed AIHubVault's type from "Hub" to "Preset Hub (default)"; recorded Multi-Hub Phase 1 completion on the Project_AIMindVaults row. (3) Linked the Phase 1 implementation result note in the Multi-Hub Architecture Design header. (4) New implementation result note (Project_AIMindVaults). (5) Rewrote root `_SESSION_HANDOFF_CLAUDE.md`. Korean/English deployment pending — the entire BasicVaults including CoreHub / AIHubVault_Minimal is auto-included in deploy. |
| R066 | 2026-04-20 | ✅ | ✅ | **Multi-Hub Phase 1 → reflected in root rules.** (1) `.claude/rules/core/_skill-router.md` — added a Multi-Hub trigger row (keywords: Core Hub, Preset Hub, CoreHub, core-sync, core-sync-all, hub-source.json, hub-marker.json, create-hub, multi-hub, Preset Hub, Core Hub, bump-version --broadcast, hubId, hubType, hub-resolver) → instructs reading the design doc + implementation result note. (2) `.claude/rules/core/_essentials.md § 4` — workspace-mode section fully rewritten: Multi-Hub tier table (Core Hub / Preset Hub / root / per-vault), mandatory Core editing workflow (CoreHub-only · `bump-version --broadcast` auto-chain · no completion report before that command runs), Preset Hub editing order, and absolute prohibitions (no Core editing on Preset/satellite · no manual hub-marker on satellites). Both files are always-injected, so future agents auto-recognize Multi-Hub triggers and apply the Core editing rules. Korean/English deployment pending — CoreHub itself is in BasicVaults and ships with deploy. |
| R065 | 2026-04-19 | – | – | **Phase 8 settings re-validation — eliminated bkit/MCP leakage.** First measurement showed Custom agents 6.3k and MCP deferred 67.3k did not drop. Diagnosis: (1) project `bkit: true` in `.claude/settings.json` overrode the global `false` → root sessions were loading 16 bkit agents. (2) Global `mcpServers` in `~/.claude.json` registered `mcp-unity` · `serena` → injected into every session. Mitigation: (1) removed the `enabledPlugins` block from the project `.claude/settings.json` (inheriting global `false` for bkit/github). (2) emptied global `mcpServers` to `{}` (removed `mcp-unity` · `serena`); backup at `~/.claude.json.backup-20260419`. (3) added `serena` MCP only to the Unity project (loads only there). (4) added 3 new `.claude/settings.json` files in select sub-vaults to enable bkit only there. Expected effect: AIMindVaults root session — Custom agents 6.3k → 0; MCP deferred drops by ~8k from removing mcp-unity (28 tools) and serena (19 tools). The remaining ~59k MCP-deferred is from Claude Desktop built-in connectors (Calendar · Gmail · MS Docs · Chrome, etc.) and must be disabled in the Desktop UI. Personal env, not a deployment target. |
| R064 | 2026-04-18 | – | – | **Phase 8 MCP/Plugins optimization complete — personal-env config.** (1) Restructured global `~/.claude/settings.json`: `mcpServers` reduced 7 → 1 (kept only `notion`); removed `playwright`, `context7`, `github` (Failed); moved `mcp-unity`, `blender`, `youtube-transcript`. Both `enabledPlugins` flipped to `false` (`bkit@bkit-marketplace`, `github@claude-plugins-official`). (2) New AIMindVaults project `.claude/settings.json` — enables `bkit` at project scope (for the relevant sub-vaults), includes `blender` · `youtube-transcript` MCP. (3) New Unity project `.claude/settings.json` — `mcp-unity` MCP only. Backup: `~/.claude/settings.json.backup-20260418`. Personal env change — not a deployment target (`–` on both Korean/English). Expected effect: MCP tools (deferred) 67.3k → ~15k, Custom agents 6.3k → ~0, baseline 170k → ~80k. |
| R063 | 2026-04-18 | ✅ | ✅ | **Removed `.forge/` directory entirely.** (1) Deleted `.forge/` from 29 vaults (AIHubVault, BasicContentsVault, all Domains/Labs/Projects) — abandoned the "external-agent output staging hub" concept. (2) Cleaned up legacy `_forge` / `.forge` references across root `CLAUDE.md`, `.cursorrules`, `.codex/CODEX.md`, `.codex/rules/vault-routing.md`, `_WORKFLOW.md`, `.sync/_WORKFLOW.md`; AIHubVault `CLAUDE.md`, `_STATUS.md`, `AGENTS.md`, `CODEX.md`, `.codex/CODEX.md`, `.codex/rules/never-do.md`, `.antigravity/SESSION_RULES.md`, `.github/copilot-instructions.md`, `CORE_CONCEPTS.md`, `README.md`, `_Standards/CONTENTS_SPEC.md`, `Contents/CONTENTS_GLOSSARY.md`, `Contents/CONTENTS_AI_RULES.md`; root `_Standards/Core/Script_Registry.md` (removed two one-off `.forge/staging/` scripts). (3) PS1 logic update — `_tools/cli/pre_sync.ps1` and `sync_workspace.ps1` switched the Hub-identification marker from `_forge/` to `.sync/.hub_marker` (consistent with the Node.js CLI). (4) The Korean/English distributions had pending uncommitted `.forge/` removals (47 / 141 files); rolled into this R063 commit. Preserved: per-vault `_VAULT-INDEX.md`, `_WORKSPACE_VERSION.md`, and historical references in plan notes under Contents (kept for record). |
| R062 | 2026-04-18 | ✅ | ✅ | **Rule-injection refactor Phase 2-C + Phase 3 complete.** (1) Phase 2-C: condensed `core/user-guidance.md` § 5 (workspace sync) and § 8 (post-edit verification) into reference-style pointers — concrete commands delegated to `_essentials.md § 4 / § 5`; user-guidance keeps detection + a one-line key. (2) Phase 3: relocated `core/vault-individualization.md` (2.1k) to `rules-archive/vault-individualization.md`; added an archive Read instruction to `commands/core/create-vault.md`; switched the "New vault" row in `core/_skill-router.md` to `/create-vault` Skill + archive Read; corrected the reference path in `core/user-guidance.md § 2` to the rules-archive path; removed `vault-individualization` from the always-injected "rest" list in root `CLAUDE.md`; updated `.claude/rules/MANIFEST.md` (removed from core table, added a Phase 3 section under rules-archive). (3) Final outcome on the remaining Phase 2-A `bulk-edit-safe`: **not created** — `encoding-safety` and `temp-file-management` remain in core (always-required safety rules; relocating to a Skill trigger risks delayed self-correction at session start). Estimated reduction from Phase 3 -2.1k (Memory files 25.1k → ~23k). Both Korean/English deployments 🕓 — `vault-individualization` is a general-purpose rule, so its relocation needs to be reflected in both. |
| R061 | 2026-04-18 | ✅ | ✅ | **Rule-injection refactor Phase 2-A complete — 6 domain rules transitioned to Skills.** Relocated 6 rules from custom/ to rules-archive/: `blender-mcp.md`, `notion-sync.md`, `serena-mcp.md`, `unity-scripting-style.md`, `unity-tools.md`, `discord-bot.md`. Added 5 Skills under `commands/custom/`: `blender-workflow`, `meshy-workflow`, `notion-record`, `discord-admin`, `unity-dev` (the latter combining unity-tools / unity-scripting-style / serena-mcp). Rewrote `_skill-router.md` to invoke Skills instead of "Read file path", and added a Phase 2-A complete section. Updated `MANIFEST.md` — custom/ shrunk 8 → 2 (only `agent-ownership` and `multivault-personalization` remain), and rules-archive/ adds the 6 Phase 2-A entries. The two custom rules left are kept since they may apply to deployment users too (agent collaboration / multi-vault personalization). **`bulk-edit-safe` Skill on hold** — its targets (`encoding-safety`, `temp-file-management`) are core safety rules whose relocation risks failed self-correction at session start. Expected total reduction ~8k tokens (6 of 12.7k custom moved). Phase 2-A complete = Skill-transition principle validated. Next: live measurement → distribution branch version → simultaneous reflection in onboarding/README. |
| R060 | 2026-04-18 | ✅ | – | **Rule-injection refactor Phase 2-A PoC — `/distribute` Skill transition.** (1) Moved `.claude/rules/custom/distribution-deploy.md` and `sync-version-priority.md` to `.claude/rules-archive/` (excluded from auto-injection). (2) New `.claude/commands/custom/distribute.md` — multi-language deployment paths, content-criteria checklist, Git push procedure, Pre/Post-Check on sync changes — all combined into one Skill. Trigger keywords: deploy, SellingVault, git push, English deployment, distribute, deploy, cli.js sync, pre-sync, etc. (3) Updated `.claude/rules/core/_skill-router.md` — destinations of the "deploy / Git push" and "sync feature change" rows switched to the `/distribute` Skill; appended a Phase 2-A status section. (4) Updated `.claude/rules/MANIFEST.md` — removed 2 from custom/, added a Phase 2-A section under rules-archive/. **Korean status set to creator-only adjustment**: those two `_skill-router.md` rows point to creator-only paths (`/distribute` Skill + archive files), which would be broken in deployment user environments → distribution needs a slimmed branch (option C). Custom-area changes are deployment-irrelevant for both Korean/English. First PoC of 7 planned Skills; Unity / Blender / Meshy / Discord / Notion to follow after measurement. |
| R059 | 2026-04-18 | ✅ | ✅ | **Rule-injection refactor Phase 2-B complete — `user-guidance.md` D-plan split.** Split the original (4.3k) into two layers by risk grade: (1) Slimmed `.claude/rules/core/user-guidance.md` to keep only the 6 high-risk sections (§ 2 manual-copy vault creation, § 4 edit-mode confusion, § 5 workspace-sync version-record omission, § 8 post-edit BAD/INDEX verification, § 10 infinite-recursion deletion, § 11 script-creation pre-approval) inline as immediate-block triggers. (2) New `.claude/rules-archive/user-guidance-detail.md` — full 12-section original (including trigger types A/B/C). (3) Extended `.claude/rules/core/_skill-router.md` — renamed the "user question / stuck" row to "user guidance — low risk", expanded trigger keywords + redirected to `rules-archive/user-guidance-detail.md`. (4) Updated `.claude/rules/MANIFEST.md` — adjusted core/ user-guidance description, added a Phase 2-B section under rules-archive/. Pure-archive move (option C) was rejected because Type-B triggers depend on immediate blocking; archive Read latency would risk mistakes. |
| R058 | 2026-04-18 | ✅ | ✅ | **Rule-injection refactor Phase 1 complete.** New `.claude/rules/core/_essentials.md` (report language, token saving, vault routing, edit modes, Post-Edit Review, note-writing frontmatter/H1/filename, session end — 8.6 KB combined). New `.claude/rules/core/_skill-router.md` (12+ trigger-keyword → rule-file mapping — 3.8 KB). The 7 absorbed files (`token-optimization`, `session-exit`, `note-writing`, `vault-routing`, `post-edit-review`, `edit-mode-separation`, `report-language`) moved to `.claude/rules-archive/`. `.claude/rules/MANIFEST.md` rewritten (core 11 / custom 10 / archive 8). Root `_STATUS.md` vault registry adds a "Path" column (vault ID → path lookup unified). Slimmed root `CLAUDE.md` 12.6 KB → 6.6 KB (47% reduction) — removed the vault-registry table in favor of a `_STATUS.md` reference, simplified keyword inference to a 2-column "keyword → vault ID" table, and added an injection-structure section. |
| R054 | 2026-04-15 | – | – | Introduced multi-language deployment structure. `distribution-deploy.md` (custom) declares the Korean/English 2-path + 2-repo arrangement, adds GitHub-Issues triage rules, and adds a multi-language operations section. `_ROOT_VERSION.md` schema gains the deploy-status columns (Korean/English). New plan note `[[20260415_english_distribution_translation_plan]]` in `Project_AIMindVaults`; the Discord/cafe beta-posting plan reflects the English-version dependency. |
| R055 | 2026-04-17 | – | – | New Discord domain vault (`Vaults/Domains_Infra/Discord/`). Pre-info-gathering for the AIMindVaults Discord-server operating decision. Added vault-registry / entry-protocol keywords in root `CLAUDE.md` and registered in root `_STATUS.md`. Began accumulating bot · channel · automation · moderation domain knowledge. |
| R056 | 2026-04-17 | – | – | Added custom skill / rule for AIMindVaults Admin Bot operations (creator-only; not a deployment-sync target). New `.claude/commands/custom/discord-manage.md` and `.claude/rules/custom/discord-bot.md`. Registered in both MANIFEST custom sections. |
| R057 | 2026-04-18 | – | – | **Rule-injection refactor Phase 0 PoC.** Introduced `.claude/rules-archive/`; moved `meshy-api.md` from custom/ to archive/ to test auto-injection exclusion. Root `CLAUDE.md` adds an "Archived rules" section + a rules-archive entry in the namespace structure. `.claude/rules/MANIFEST.md` removes meshy-api from custom and adds a "rules-archive/" section. `.claude/settings.local.json` adds 13 `permissions.deny` entries (catastrophic deletion · force-push · SellingVault writes blocked) + `skipAutoPermissionPrompt: true`. New plan note for the rule-injection refactor. |

## Legacy Schema (before 2026-04-15)

Records prior to the deployment-status columns. To check past reflection, trace the deployment repo's git history.

| Version | Date | Change |
|---------|------|--------|
| R053 | 2026-04-13 | Removed all remaining PS1 references. Codex skills (3), CODEX.md (28), .codex/rules/ (84), .codex/skills/session-end (27), .github/copilot-instructions (28), .antigravity/SESSION_RULES (28), AGENT_ONBOARDING_CLAUDE/CODEX, .codex/rules/edit-scope, agent-ownership, Hub _WORKFLOW, Hub_Sync_Targets, TOOLS_INDEX — 200+ files migrated to Node.js CLI references. |
| R052 | 2026-04-13 | Created `docs/` (architecture, cli-reference, customization). 6 core commands switched PS1 → Node.js CLI. AGENT_ONBOARDING.md PS1 references converted + docs cross-link. README.md adds docs reference. |
| R051 | 2026-04-13 | 8 core/custom rules switched PS1 → Node.js CLI references. Removed PS1 entirely. Added standards command. Reorganized Script_Registry. |
| R050 | 2026-04-13 | Removed TestVault. Registered 4 vaults in their `CLAUDE.md` registry + routing keywords. Added `Domains_3D`, `Domains_VCS`, `Domains_AI_Asset` categories. |
| R049 | 2026-04-13 | Standardized frontmatter `tags`. Flattened hierarchical tags, converted PascalCase → kebab-case, removed vault-identifier tags (~696 files in two passes). `note-writing.md` adds a tag rule — base format + user-override structure. `AGENT_ONBOARDING.md § 8` adds a type/tag system explanation. |
| R048 | 2026-04-13 | Processed 86 empty types. New `folder-index` core type (note-writing.md). Domain.md/Project.md (51) → folder-index, legacy combat-toolkit notes (27) → design, Grok logs (5) → reference, individuals (3) → plan / knowledge / study-note. |
| R047 | 2026-04-13 | Fixed `master_index_build.ps1` Resolve-Path trailing-backslash bug (added TrimEnd). New core `/sync-all` skill — bulk-sync workspace across all satellite vaults. Registered in MANIFEST. |
| R046 | 2026-04-13 | Standardized frontmatter `type`. `note-writing.md` adds a 21-item core-type list + vault-specific extension rule. Synonym consolidation (72: standards→standard, knowledge-note/domain→knowledge, planning-note→plan, research-note→research), one-shot type absorption (12), removed 156 EXAMPLE_JUGGL notes from satellite vaults. ObsidianDev/Unity CLAUDE.md declare vault-specific types. |
| R045 | 2026-04-13 | New `Vaults/Domains_Dev/JavaScript/` vault. Added JavaScript registry / routing keywords to root `CLAUDE.md` and `_STATUS.md`. |
| R044 | 2026-04-13 | `vault-individualization.md` adds collection-scope / boundary to the CLAUDE.md required items. `vault-routing.md` adds a rule that routing decisions reference the candidate vault's CLAUDE.md collection scope. |
| R043 | 2026-04-08 | New core `user-guidance.md` rule. 12 user-confusion situations across core features + agent guidance. Registered in MANIFEST. `create-vault.md`, `open-vault.md` add a "no Obsidian-unregistered-vault URI" rule. `AGENT_ONBOARDING.md § 16` adds a vault-registration guide. |
| R042 | 2026-04-08 | Added "Infinite-recursion path deletion (Incident Rule)" to `temp-file-management.md`. PowerShell flatten-and-delete is the 1st choice; robocopy mirror is the 2nd. Listed methods that fail (7z, Remove-Item, rd, .NET Delete). |
| R041 | 2026-04-08 | New `Vaults/Domain_Art/ArtInsight/` vault. Updated root `AGENTS.md`, `CLAUDE.md`, `_STATUS.md` registries / routing keywords; individualized ArtInsight entry / status / content rules; first note authored. |
| R040 | 2026-04-08 | Added "content indexer first" mandatory rule. `token-optimization.md § 0` (indexer → fallback order); `vault-routing.md` mandates an index build on first access; `vault-individualization.md` adds an initial-build step after vault creation. |
| R039 | 2026-04-08 | Vault rename CodeStyle → AI_Coding. Updated root CLAUDE.md registry / routing, root _STATUS.md registry. Replaced internal tags vault-wide. |
| R038 | 2026-04-08 | Strengthened content-indexing completion enforcement. The AIHub `.sync/_tools/cli/post_note_edit_review.ps1` now runs the indexer in a separate PowerShell process to stabilize success detection; the root `.codex/skills/create-video-note`, `create-article-note`, `create-pdf-note` add `POST_EDIT_INDEX_UPDATED=1` verification + manual fallback procedures. |
| R037 | 2026-04-08 | Strengthened content-indexing rules. `post-edit-review.md` adds a mandatory post-add/edit indexing condition (`POST_EDIT_INDEX_UPDATED=1`); the `/reindex` skill path is updated to the `.sync/_tools/cli/` structure. |
| R036 | 2026-04-08 | New Codex-only source-note pipeline. Created `.codex/skills/create-article-note/`, `.codex/skills/create-pdf-note/` and extended `.codex/skills/create-video-note/` to the same baseline. |
| R035 | 2026-04-06 | Promoted `/open-notes` skill from custom/ to core/. Added a 3-stage safety net: active-vault detection · path conversion · file-existence check. Registered in MANIFEST. |
| R034 | 2026-04-06 | New `AGENT_ONBOARDING.md` — universal agent onboarding doc. Environment overview, condensed 14 core conventions, references to rule-file paths. |
| R033 | 2026-04-06 | `distribution-sync.md` removes Juggl_StyleGuide deployment; `juggl-style-sync.md` switched to per-vault management. Bulk vault rename `docs/` → `.docs/`, deleted `recent_files.txt`, moved `Juggl_StyleGuide/` to per-vault `Contents/`. |
| R032 | 2026-04-05 | New `/reindex` skill (`.claude/commands/core/reindex.md`) — rebuild content index for one or all vaults. Registered in MANIFEST. |
| R031 | 2026-04-05 | New `/open-notes` skill (`.claude/commands/custom/open-notes.md`) — open multiple notes in new tabs via local-rest-api. Registered in the global CLAUDE.md skill list. |
| R030 | 2026-04-02 | `session-exit.md` adds a rule that per-vault handoffs leave a reference in the root handoff. |
| R029 | 2026-04-01 | Split session handoff per agent: `_SESSION_HANDOFF.md` → `_SESSION_HANDOFF_CLAUDE.md` + `_SESSION_HANDOFF_CODEX.md`. Updated `session-exit.md` rules and `CLAUDE.md` entry protocol. |
| R028 | 2026-04-01 | Created a new vault and registered it. Added `Projects_Game` category and updated root `AGENTS.md`, `CLAUDE.md`, `_STATUS.md`, `.claude/rules/core/vault-individualization.md`. |
| R027 | 2026-04-01 | Created another new vault. Added registry / routing entries to root `AGENTS.md`, `CLAUDE.md`, `_STATUS.md`. |
| R026 | 2026-04-01 | `note-writing.md` clarifies the frontmatter `agent` field rule: record cumulative working agents, not just the most recent. |
| R025 | 2026-03-23 | New vault-individualization rule (`.claude/rules/core/vault-individualization.md`) — standardize name / classification / CLAUDE.md / tags on vault creation. |
| R024 | 2026-03-23 | New multi-vault personalization rule (`.claude/rules/custom/multivault-personalization.md`) — agent / plug-in / skill custom configuration. |
| R023 | 2026-03-21 | New "Personal" category + Diary vault. Registered in root CLAUDE.md and _STATUS.md. Added routing keywords. |
| R022 | 2026-03-21 | Introduced the session-handoff system. New root `_SESSION_HANDOFF.md`, `session-exit.md` adds handoff-write rule, root `CLAUDE.md` entry protocol adds reading the handoff. |
| R021 | 2026-03-21 | Created two new vaults to prepare for content separation from AIHubVault. |
| R020 | 2026-03-21 | `note-writing.md` adds the mandatory wikilink rule — when creating a new note, include at least one `[[wikilink]]` to a related note in the same vault. |
| R019 | 2026-03-21 | `note-writing.md` adds H1-title rules — URI-reserved characters and emoji are forbidden (mandatory). Filenames also forbid emoji. |
| R018 | 2026-03-21 | New `Vaults/Domains_Infra/Search/` vault. Search-engine / indexing / text-matching domain knowledge hub. Registered in root CLAUDE.md and _STATUS.md. |
| R017 | 2026-03-21 | New `Vaults/Domains_Infra/CICD/` vault. CI/CD and deployment-sync domain knowledge hub. Registered in root CLAUDE.md and _STATUS.md. |
| R016 | 2026-03-21 | Codex Desktop App migration. New root + 11-vault `AGENTS.md` (2-stage routing + execution structure). Converted playbooks → `.codex/skills/` SKILL.md format. Migrated `.antigravity/workflows/` → `.codex/skills/`. New `agent-ownership.md` conflict-prevention rule. Marked legacy CODEX.md / `.antigravity` deprecated. |
| R015 | 2026-03-19 | New `.claude/rules/vault-routing.md`: vault-routing mandatory rule. No direct work in BasicContentsVault; vault-registry check required before placing content. |
| R014 | 2026-03-19 | Switched agent rules to canon-reference. Deleted 8 duplicate rules across Cursor/Codex/Antigravity → `.claude/rules/` canon reference + per-agent `agent-rules` file. Internal Codex/Antigravity entry points in AIHubVault also reference the canon. |
| R013 | 2026-03-19 | Redesigned root `_STATUS.md` to a lightweight registry structure (Now/Next/Blocked removed → vault type + content + working-agent date). Updated `session-exit.md` root-update rule. Registered all 11 vaults. |
| R012 | 2026-03-18 | New `Vaults/Lab_Infra/ObsidianDev/` vault. New "Lab" category (Domain + Project hybrid). Added a Labs section to root CLAUDE.md. |
| R011 | 2026-03-18 | New `.claude/commands/auto-organize.md`: auto-classify folders during note/vault creation. AI proposes → user approves. |
| R010 | 2026-03-18 | New `Vaults/Domains_Infra/Notion/` vault. Added registry to root CLAUDE.md and a section to _STATUS.md. |
| R009 | 2026-03-18 | New `.claude/commands/create-vault.md`: vault-creation skill. BasicContentsVault-based clone + follow-up procedure definition. |
| R008 | 2026-03-18 | `note-writing.md` adds the "no metaphor / simile" rule. Task names and titles describe content directly. |
| R007 | 2026-03-17 | `juggl-style-sync.md` clarifies that the Juggl `local:` value is the filename (not the H1 title). |
| R006 | 2026-03-17 | `note-writing.md` adds the markdown-bold + parentheses rendering rule. The pattern `**text(parens)**` is forbidden. |
| R005 | 2026-03-17 | New `README.md`: multi-vault system intro, AI rule architecture, quick-start guide (git-distribution entry point). |
| R004 | 2026-03-17 | New `.claude/rules/token-optimization.md`: token-saving and execution-delegation mandatory rule. |
| R003 | 2026-03-17 | `edit-mode-separation.md` adds a mandatory rule: record any root-level edits in `_ROOT_VERSION.md`. |
| R002 | 2026-03-17 | 3 new `.claude/rules/`: temp-file-management, distribution-sync, script-creation-approval. |
