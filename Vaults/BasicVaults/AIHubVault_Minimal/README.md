# AIHubVault_Minimal

AIMindVaults Multi-Hub 아키텍처의 **Minimal Preset Hub** — Core 6 플러그인만 포함된 최소 Preset. Custom 번들 없음.

Core 계층 변경 시 CoreHub 에서 `bump-version --broadcast` 로 자동 수신. 자세한 역할·규칙은 `CLAUDE.md` 참조.
