---
tags:
  - Metatype: workflow
updated: 2026-03-19
---

# BasicContentsVault — Vault Clone Source Template

> **This vault is the clone source (template) for the `/create-vault` skill.**
> Direct content work is prohibited. Used only for creating new vaults.

## If the vault name is BasicContentsVault

This vault is the **clone source template** for `/create-vault`.
- Direct content work is prohibited
- Workspace is synchronized from AIHubVault via `sync_workspace.ps1`
- Related: `/create-vault` skill

## If the vault name is NOT BasicContentsVault

This vault has not been initialized after cloning. **Perform the following immediately:**
1. Update this CLAUDE.md's title/role/structure to match the actual vault purpose
2. Update `_STATUS.md`'s vault role accordingly
3. Register this vault in the root `_STATUS.md` vault registry (if not registered)
